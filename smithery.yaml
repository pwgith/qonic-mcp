# Smithery registry configuration.
# The MCP server is hosted on Vercel – Smithery only needs the URL.
startCommand:
  type: http
  url: "https://qonic-mcp.vercel.app"
  configSchema:
    type: object
    title: Qonic MCP Configuration
    description: Configuration for the Qonic MCP server
    properties: {}
